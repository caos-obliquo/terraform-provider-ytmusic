[package]
name = "rym-to-genre"
version = "0.1.0"
edition = "2021"
description = "Convert RYM text dumps to genre-to-playlist JSON entries"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
